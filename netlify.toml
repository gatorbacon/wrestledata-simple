[build]
  base    = "frontend/hs-ky-ui"
  command = "npm run build"
  publish = "frontend/hs-ky-ui/dist"

# SPA routing — send all paths to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
