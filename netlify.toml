[build]
  publish = "."

# Temporary tournament redirect: / → /ncaa_live.html
[[redirects]]
  from = "/"
  to = "/ncaa_live.html"
  status = 302

# Cache historical replay data for 1 hour (never changes)
[[headers]]
  for = "/data/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"
